ggplot(data, aes(x = parameter, y = sum_value)） +
    geom_bar(stat="identity", fill = color1, colour = color2)+   #stat="identity"
    geom_errorbar(aes(ymin = sum_value - se, ymax = sum_value + se), width = 0.2)
    
